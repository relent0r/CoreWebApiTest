apiVersion: v1
kind: Pod
metadata:
  name: corewebapi-__CI_JOB_ID__
  labels:
    app: reveraapi-__CI_JOB_ID__
spec:
  containers:
    - name: corewebapi-__CI_JOB_ID__
      image: relent0r/corewebapi:prod
      imagePullPolicy: Always
      ports:
      - containerPort: 80
        name: http
        protocol: TCP
